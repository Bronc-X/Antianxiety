# 🍎 iOS 全链路验收测试计划 (Solo Tester Edition)

> **导读**：这是专为 iOS 端定制的“Notion 风格”仪表盘。
> 核心逻辑：**左手拿 iPhone (或 Simulator)，右手跑 SQL**。
> 目标：确认 .ipa 在苹果生态下是优雅的、顺滑的、符合 HIG 的。

---

## 🛠️ 准备工作 (Ready)

1. **环境准备**
   - 确保 Xcode 已安装且 `npx cap sync ios` 已执行。
   - 准备好 Simulator (推荐 iPhone 15/16 Pro) 或真机。

2. **获取你的 User ID (UID)**
   - 同 Web 端，在 Supabase 后台获取。

---

## A+. 2025 前沿 UI 验收 (Calm Luxury) 💎

> **目标**：验证本次 UI 升级的三大支柱 (Feel, Motion, Native)。

- [ ] **Phase 1: 质感 (The Feel)**
   - **操作**：观察背景与文字。
   - **预期**：
     1. **活体纹理**：背景有胶片噪点在微弱涌动 (Alive Texture)。
     2. **Fluid Typography**：字体大小随屏幕宽度无级缩放。

- [ ] **Phase 2: 动态 (The Motion)**
   - **操作**：在首页缓慢向下滚动 (Scrollytelling)。
   - **预期**：
     1. 标题 "Silence the Noise" 应随滚动渐隐并上浮。
     2. 背景网格线应有视差滚动效果 (Parallax)。

- [ ] **Phase 3: 面板微件 (iOS Widget)**
   - **操作**：(需先手动添加 Target) 桌面长按 -> 左上角 "+" -> 搜索 "AntiAnxiety"。
   - **预期**：
     1. 看到 "Daily Balance" 小组件预览。
     2. 添加后，点击表情能直接深层链接 (Deep Link) 到对应状态。

---

## A. 启动与原生体验 (Launch & Native Feel)

> **目标**：验证它是否有“果味” (Apple-like)。

- [ ] **A1. 启动与安全区域 (Safe Area)**
   - **操作**：启动 App。
   - **预期**：
     1. Splash Screen 完美衔接首页。
     2. 顶部刘海/灵动岛区域不遮挡 Logo 或标题。
     3. 底部 Home Indicator (黑/白条) 区域有足够的 Padding，内容不被遮挡。

- [ ] **A2. 橡皮筋效果 (Overscroll)**
   - **操作**：在首页或列表页顶部/底部用力拖拽。
   - **预期**：应该有 iOS 标志性的回弹效果 (Rubber Banding)，而不是生硬的静止。

- [ ] **A3. 侧滑返回 (Swipe Back)**
   - **操作**：进入子页面 (如“每日校准”) -> 屏幕左侧边缘向右滑。
   - **预期**：流畅返回上一页。如果这是 Web 单页应用，需确认手势是否被正确捕获 (Router Back)。

---

## B. 每日校准 (Daily Calibration) - 触感篇

> **目标**：Haptics 决定了 App 的“高级感”。

- [ ] **B1. 触觉反馈 (Haptics)**
   - **操作**：点击底部导航栏 (Bottom Tab) 切换页面。
   - **预期**：指尖应感到微弱的 **Taptic Engine 震动** (Selection Style)。这是区分 Web 和 Native 的关键细节。

- [ ] **B2. 键盘与输入**
   - **操作**：在 Weekly 评估中点击输入框。
   - **预期**：
     1. iOS 原生键盘弹起。
     2. 页面自动上推 (Keyboard Avoidance)，输入框不被键盘遮挡。
     3. 键盘右上角可能有“完成”或点击空白处可收起键盘。

---

## C. 网络与异常 (Resilience) - iOS 特供

- [ ] **C1. 控制中心断网**
   - **操作**：下拉控制中心 -> 关闭 Wi-Fi 和 蜂窝数据 -> 提交问卷。
   - **预期**：优雅的错误提示 (Alert 或 Toast)，而不是 App 崩溃 (Crash)。

- [ ] **C2. 权限弹窗**
   - **操作**：如果是第一次安装。
   - **预期**：如果是为了 AI 语音输入，应在点击麦克风时才弹出“麦克风权限”，而不是启动时立刻弹窗 (那是流氓软件行为)。

---

## D. 核心业务验证 (Core Business)

> **目标**：逻辑与 Web 端保持一致。

- [ ] **D1. 正常打卡数据流**
   - **操作**：完成一次 Daily Calibration。
   - **验证 (SQL)**：
     ```sql
     -- 确认 iOS 端提交的数据
     select count(*) from user_scale_responses 
     where user_id = '<UID>' and response_date = current_date;
     ```

- [ ] **D2. AI 对话渲染**
   - **操作**：进入 AI 聊天页。
   - **预期**：在 iOS Safari 核心下，Markdown 渲染正常，字体应为系统默认 (SF Pro)，阅读体验舒适。

---

## E. 视觉与生态 (Design & Ecosystem)

- [ ] **E1. 动态字体 (Dynamic Type)**
   - **操作**：iOS 设置 -> 显示与亮度 -> 文字大小 -> 调大。
   - **预期**：App 内文字大小随之变化 (如果支持) 或至少保持排版整洁不乱。

- [ ] **E2. 深色模式 (Dark Mode)**
   - **操作**：控制中心 -> 开启深色模式。
   - **预期**：
     1. 背景变为深邃的黑/灰 (#000000 / #1C1C1E)。
     2. 不应出现 Web 常见的“白色光标”或“白色滚动条背景”。
     3. 状态栏文字自动变白。

## F. 账号与会话 (Auth & Session)

> **目标**：登录态稳定，不出现“假登录”。

- [ ] **F1. 登出后访问**
   - **操作**：在设置/侧边栏登出 -> 重新打开 App。
   - **预期**：回到登录页或引导页，不应直进 Dashboard。

- [ ] **F2. 会话过期**
   - **操作**：清理本地存储 (或长时间不操作) 后刷新/重启 App。
   - **预期**：提示重新登录；不会白屏或卡死。

- [ ] **F3. 多端同步**
   - **操作**：电脑端完成一次 Daily；iOS 端刷新首页。
   - **预期**：能看到今日状态变化，数据同步。

## G. 生命周期 (Lifecycle)

> **目标**：切后台、来电打断后都能正常恢复。

- [ ] **G1. 退后台再回来**
   - **操作**：答题中途按 Home/上划回桌面 -> 1 分钟后返回。
   - **预期**：仍停留在原题目，或明确提示“是否继续”。

- [ ] **G2. 强杀重启**
   - **操作**：在应用切换器中上划杀掉 -> 重新打开 App。
   - **预期**：不崩溃；若中途答题，允许继续或安全回到首页。

## H. 时间与边界 (Time & Boundary)

> **目标**：日/周/月切换准确。

- [ ] **H1. 跨天边界**
   - **操作**：临近 24:00 完成 Daily；过零点再进入首页。
   - **预期**：新的一天可再次打卡，不会把昨天当今天。

- [ ] **H2. 时区影响**
   - **操作**：系统设置切换时区 -> 返回 App。
   - **预期**：Daily 的“今日”逻辑以本地时间为准。

## I. 重复与幂等 (Idempotency)

> **目标**：重复操作不会产生脏数据。

- [ ] **I1. 连点提交**
   - **操作**：提交按钮连续点 2-3 次。
   - **预期**：只产生一次提交记录。

- [ ] **I2. 今日已完成**
   - **操作**：已完成 Daily 后再次进入入口。
   - **预期**：提示“已完成”或展示结果页，不再出题。

## J. 屏幕方向 (Orientation)

> **目标**：横竖屏都不出错。

- [ ] **J1. 旋转屏幕**
   - **操作**：横竖屏切换 (真机或模拟器)。
   - **预期**：如果锁定竖屏，界面不旋转；若支持横屏，布局不乱、答题进度不丢。

## 🧹 测试收尾

清理数据同 Web 端：
```sql
delete from user_scale_responses where user_id = '<UID>';
delete from daily_wellness_logs where user_id = '<UID>';
```
