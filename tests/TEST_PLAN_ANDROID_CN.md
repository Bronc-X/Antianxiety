# 🤖 Android 全链路验收测试计划 (Solo Tester Edition)

> **导读**：这是专为 Android 端定制的“Notion 风格”仪表盘。
> 核心逻辑：**左手拿手机(或模拟器)，右手敲 ADB/SQL**。
> 目标：确认 .apk 在安卓生态下是活的、准的、有质感的。

---

## 🛠️ 准备工作 (Ready)

1. **环境准备**
   - 确保 Android Studio 已安装且 `npx cap sync android` 已执行。
   - 准备好模拟器 (推荐 Pixel 6/7, API 33+) 或真机 (通过 USB 调试连接)。

2. **获取你的 User ID (UID)**
   - 在 Supabase 后台 `auth.users` 表里找到你的 ID。
   - 或在 Logcat 中搜索 `User ID:` (如果已添加相关日志)。

---

## A. 启动与原生体验 (Launch & Native Feel)

> **目标**：验证它不像一个“套壳网页”，而像一个原生 APP。

- [ ] **A1. 冷启动 (Cold Start)**
   - **操作**：杀掉后台进程 -> 点击图标启动。
   - **预期**：
     1. 展示 **Warm Cream (#FAF6EF)** 背景的 Splash Screen。
     2. 不会出现长时间白屏。
     3. 状态栏 (Status Bar) 颜色与应用背景融合，不应出现突兀的黑条。

- [ ] **A2. Android 返回手势 (Back Gesture)**
   - **操作**：进入“每日校准” -> 答题页 -> 屏幕左侧/右侧边缘向内滑 (系统返回)。
   - **预期**：应该返回上一页（首页），而不是直接退出 APP 或无反应。

- [ ] **A3. 沉浸式导航栏**
   - **操作**：观察底部小白条 (Navigation Bar) 区域。
   - **预期**：内容应该延伸到底部 (Edge-to-Edge)，底部导航栏背景应为透明或半透明，不应被黑色遮挡。

---

## A+. 2025 前沿 UI 验收 (Calm Luxury) 💎

> **目标**：验证本次 UI 升级的三大支柱 (Feel, Motion, Native)。

- [ ] **Phase 1: 质感 (The Feel)**
   - **操作**：仔细观察背景和排版。
   - **预期**：
     1. **活体纹理 (Alive Texture)**：背景不是纯色，而是有微弱的胶片噪点在“呼吸” (bg-noise animation)。
     2. **流体排版 (Fluid Type)**：在不同尺寸模拟器上，字体大小应自然缩放，无断裂感。

- [ ] **Phase 3: 面板微件 (Android Widget)**
   - **操作**：回到桌面 -> 长按空白处 -> Widgets -> 找到 "AntiAnxiety" -> 拖拽 "Daily Balance" 到桌面。
   - **预期**：
     1. 微件成功展示，带有圆角和玻璃质感背景。
     2. 点击表情 (Good/Neutral/Bad)，能直接唤起 App 并跳转/传递参数（需 Logcat 确认 Intent）.

---

## B. 每日校准 (Daily Calibration) - 触控篇

> **目标**：验证触摸反馈是否符合 Android 高级质感。

- [ ] **B1. 按钮波纹 (Ripple Effect)**
   - **操作**：点击首页“每日校准”按钮。
   - **预期**：手指按下时，按钮应有微弱的 **Ripple (水波纹)** 或 **Scale (缩放)** 反馈。

- [ ] **B2. 键盘交互**
   - **操作**：在文本输入题 (如 Weekly PSS-4) 点击输入框。
   - **预期**：
     1. 软键盘平滑弹起，依然能看到输入框 (此为 `windowSoftInputMode` 配置)。
     2. 点击空白处或“完成”，键盘收起流畅。

---

## C. 网络与异常 (Resilience) - Android 特供

> **目标**：在不稳定的 4G/5G 环境下也能存活。

- [ ] **C1. 飞行模式提交**
   - **操作**：答题到最后一题 -> 下拉通知栏开启“飞行模式” -> 点击提交。
   - **预期**：Toast 提示“网络连接失败”或“已离线”，数据未丢失 (本地暂存)。

- [ ] **C2. 后台被杀恢复**
   - **操作**：答题中途 -> 切回桌面 -> 打开其他大内存应用 (如相机/游戏) 挤掉内存 -> 重新打开 App。
   - **预期**：应用重新热启动，理想情况下恢复到刚才的页面 (取决于 Capacitor 状态恢复配置)，最差情况回到首页但不报错。

---

## D. 核心业务验证 (Core Business)

> **目标**：逻辑与 Web 端保持一致。

- [ ] **D1. 正常打卡数据流**
   - **操作**：完成一次 Daily Calibration。
   - **验证 (SQL)**：
     ```sql
     -- 确认安卓端提交的数据 source 正确
     select count(*) from user_scale_responses 
     where user_id = '<UID>' and response_date = current_date;
     ```

- [ ] **D2. 权限请求**
   - **操作**：由于是 Webview 壳，一般不需要额外敏感权限。
   - **预期**：不应无故弹出“位置”、“通讯录”等权限申请。

---

## E. 兼容性 (Compatibility)

- [ ] **E1. 字体缩放**
   - **操作**：系统设置 -> 显示 -> 字体大小 -> 调到最大。
   - **预期**：App 内文字可读，没有严重堆叠或溢出屏幕。

- [ ] **E2. 深色模式 (Dark Mode)**
   - **操作**：下拉通知栏 -> 开启“深色主题”。
   - **预期**：
     1. 背景变为 **#0a0a0a** (极简黑)。
     2. 文字反白，无“黑底黑字”现象。
     3. 底部导航栏适配深色。

## F. 账号与会话 (Auth & Session)

> **目标**：登录态稳定，不出现“假登录”。

- [ ] **F1. 登出后访问**
   - **操作**：在设置页登出 -> 重新打开 App。
   - **预期**：回到登录页或引导页，不应直进 Dashboard。

- [ ] **F2. 会话过期**
   - **操作**：清理 App 数据 (或长时间不操作) 后启动 App。
   - **预期**：提示重新登录；不会白屏或卡死。

- [ ] **F3. 多端同步**
   - **操作**：电脑端完成一次 Daily；安卓端刷新首页。
   - **预期**：能看到今日状态变化，数据同步。

## G. 生命周期 (Lifecycle)

> **目标**：切后台或被打断后能恢复。

- [ ] **G1. 退后台再回来**
   - **操作**：答题中途按 Home 键 -> 1 分钟后返回。
   - **预期**：仍停留在原题目，或明确提示“是否继续”。

- [ ] **G2. 来电/通知打断**
   - **操作**：答题中途接来电或下拉通知栏。
   - **预期**：恢复后不丢进度，不会崩溃。

## H. 时间与边界 (Time & Boundary)

> **目标**：日/周/月切换准确。

- [ ] **H1. 跨天边界**
   - **操作**：临近 24:00 完成 Daily；过零点再进入首页。
   - **预期**：新的一天可再次打卡，不会把昨天当今天。

- [ ] **H2. 时区影响**
   - **操作**：系统设置切换时区 -> 返回 App。
   - **预期**：Daily 的“今日”逻辑以本地时间为准。

## I. 重复与幂等 (Idempotency)

> **目标**：重复操作不会产生脏数据。

- [ ] **I1. 连点提交**
   - **操作**：提交按钮连续点 2-3 次。
   - **预期**：只产生一次提交记录。

- [ ] **I2. 今日已完成**
   - **操作**：已完成 Daily 后再次进入入口。
   - **预期**：提示“已完成”或展示结果页，不再出题。

## J. 屏幕方向 (Orientation)

> **目标**：横竖屏都不出错。

- [ ] **J1. 旋转屏幕**
   - **操作**：横竖屏切换 (真机或模拟器)。
   - **预期**：如果锁定竖屏，界面不旋转；若支持横屏，布局不乱、答题进度不丢。

## 🧹 测试收尾

清理数据同 Web 端：
```sql
delete from user_scale_responses where user_id = '<UID>';
delete from daily_wellness_logs where user_id = '<UID>';
```
