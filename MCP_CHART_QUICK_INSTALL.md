# MCP Chart Server 快速安装指南

本指南提供两种快速安装方式，无需下载 zip 文件。

## 🚀 方式一：使用 npm 包（推荐，最简单）

这是最快的方式，直接通过 npm 安装，无需克隆仓库。

### 步骤 1: 配置 Cursor MCP 设置

1. **打开 Cursor 设置**
   - 按 `Ctrl + Shift + P` 打开命令面板
   - 输入 `Preferences: Open User Settings (JSON)`
   - 或者手动找到配置文件

2. **找到 MCP 配置文件**

   **方法 A：通过文件资源管理器打开（最简单）**
   
   1. 按 `Win + R` 键（Win 键就是 Windows 徽标键）
   2. 在弹出的"运行"对话框中输入：
      ```
      %APPDATA%\Cursor\User\globalStorage\saoudrizwan.claude-dev\settings
      ```
   3. 按回车，会自动打开这个文件夹
   4. 如果文件夹不存在，就手动创建它
   
   **说明**：`%APPDATA%` 是 Windows 的环境变量，实际路径通常是：
   ```
   C:\Users\你的用户名\AppData\Roaming\Cursor\User\globalStorage\saoudrizwan.claude-dev\settings
   ```
   
   **方法 B：通过 Cursor 命令面板**
   
   1. 在 Cursor 中按 `Ctrl + Shift + P`
   2. 输入 `Preferences: Open User Settings (JSON)`
   3. 这会打开一个 JSON 配置文件
   4. 查看这个文件的路径，MCP 配置文件在同一个目录下
   
   **配置文件名称**：`cline_mcp_settings.json`

3. **添加配置**

   打开配置文件，添加以下内容：

   ```json
   {
     "mcpServers": {
       "mcp-server-chart": {
         "command": "cmd",
         "args": ["/c", "npx", "-y", "@antv/mcp-server-chart"]
       }
     }
   }
   ```

4. **重启 Cursor**

   完全关闭并重新打开 Cursor，MCP 服务器会自动连接。

### ✅ 完成！

首次使用时，`npx` 会自动下载并缓存包，之后就可以直接使用了。

---

## 🔧 方式二：从 GitHub 克隆到本地（适合开发/自定义）

如果你想从 GitHub 仓库克隆并本地运行：

### 步骤 1: 克隆仓库

在 Cursor 终端中执行：

```powershell
# 进入项目目录（或你想要的目录）
cd C:\Users\38939\Desktop

# 克隆仓库
git clone https://github.com/antvis/mcp-server-chart.git

# 进入目录
cd mcp-server-chart
```

### 步骤 2: 安装依赖

```powershell
# 安装依赖
npm install

# 构建项目
npm run build
```

### 步骤 3: 配置 Cursor MCP

编辑 Cursor 的 MCP 配置文件，指向本地安装的版本：

```json
{
  "mcpServers": {
    "mcp-server-chart": {
      "command": "node",
      "args": [
        "C:\\Users\\38939\\Desktop\\mcp-server-chart\\build\\index.js"
      ]
    }
  }
}
```

**注意**：将路径替换为你实际克隆的路径。

### 步骤 4: 重启 Cursor

重启 Cursor 后，MCP 服务器会使用本地版本。

---

## 🎯 推荐方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **npm 包** | ✅ 最简单<br>✅ 自动更新<br>✅ 无需维护 | ❌ 无法自定义 | 日常使用 |
| **GitHub 克隆** | ✅ 可自定义<br>✅ 可调试<br>✅ 可修改源码 | ❌ 需要手动更新<br>❌ 需要维护 | 开发/定制 |

## 🔍 验证安装

安装完成后，在 Cursor 中：

1. **检查 MCP 连接状态**
   - 查看 Cursor 的 MCP 服务器状态
   - 应该显示 "mcp-server-chart" 已连接

2. **测试功能**
   - 在对话中尝试："帮我生成一个折线图，显示 [1,2,3,4,5] 的数据"
   - 如果成功生成图表，说明安装成功

## ⚙️ 可选配置

### 自定义图表服务

如果需要使用私有部署的图表生成服务：

```json
{
  "mcpServers": {
    "mcp-server-chart": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@antv/mcp-server-chart"],
      "env": {
        "VIS_REQUEST_SERVER": "https://your-server.com/api/chart"
      }
    }
  }
}
```

### 禁用特定工具

```json
{
  "mcpServers": {
    "mcp-server-chart": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@antv/mcp-server-chart"],
      "env": {
        "DISABLED_TOOLS": "generate_fishbone_diagram,generate_mind_map"
      }
    }
  }
}
```

## 🐛 故障排查

### 问题：找不到配置文件

**解决**：
1. 在 Cursor 中按 `Ctrl + Shift + P`
2. 输入 `Preferences: Open User Settings (JSON)`
3. 查看文件路径，MCP 配置通常在相同目录下

### 问题：MCP 服务器未连接

**解决**：
1. 检查 JSON 格式是否正确（无语法错误）
2. 确认路径正确（如果使用本地版本）
3. 重启 Cursor
4. 检查 Node.js 是否已安装：`node --version`

### 问题：命令执行失败

**解决**：
1. 确认已安装 Node.js：`node --version`
2. 确认 npm 可用：`npm --version`
3. 如果使用本地版本，确认已运行 `npm install` 和 `npm run build`

## 📚 更多信息

- GitHub 仓库：https://github.com/antvis/mcp-server-chart
- npm 包：https://www.npmjs.com/package/@antv/mcp-server-chart

